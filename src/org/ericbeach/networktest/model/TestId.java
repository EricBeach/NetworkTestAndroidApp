package org.ericbeach.networktest.model;

/**
 * Enum to represent the different test ids (i.e., the different network tests that
 * can be performed).
 * @author Eric Beach (ebeach@google.com)
 */
public enum TestId {
  DNS_POISONING,
  HTTP_FILTERING,
  GOOGLE_AVAILABILITY
}
