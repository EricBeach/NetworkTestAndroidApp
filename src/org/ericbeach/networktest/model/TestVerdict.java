package org.ericbeach.networktest.model;

/**
 * Enum to represent the possible test verdicts (i.e., the outcomes of the network tests).
 * @author Eric Beach (ebeach@google.com)
 */
public enum TestVerdict {
  NO_PROBLEM,
  PROBLEM,
  TEST_INCOMPLETE
}
